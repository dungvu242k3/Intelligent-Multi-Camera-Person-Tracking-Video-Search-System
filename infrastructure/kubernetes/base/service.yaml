apiVersion: v1
kind: Service
metadata:
  name: gateway
spec:
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    app: gateway
---
apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: web
